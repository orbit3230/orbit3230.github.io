---
layout: post
title: "[인공지능수학] 9주차 - 적분"
excerpt: "부정적분, 치환적분과 부분적분, 정적분, 적분의 응용"

tags:
  - [인공지능수학, Math]

toc: true

date: 2025-10-30
last_modified_at: 2025-11-03
---
## 적분
### 1. 부정적분
- 함수 `y` = `f(x)`에 대하여 `F'(x)` = `f(x)`를 만족하는 함수 `F(x)`를 함수 `f(x)`의 **역도함수**(antiderivative)라 하고 다음과 같이 나타낸다.  
`∫ f(x) dx = F(x) + C` (단, `C`는 적분상수)  
이 때 `∫ f(x) dx`를 함수 `f(x)`의 **부정적분**(indefinite integral)이라 한다.  

<br>

- 두 함수 `f(x)`, `g(x)`에 대해 다음이 성립한다.  
  - (1) `∫ (f(x) + g(x)) dx` = `∫ f(x) dx + ∫ g(x) dx`
  - (2) `∫ (f(x) - g(x)) dx` = `∫ f(x) dx - ∫ g(x) dx`
  - (3) `∫ c f(x) dx` = `c ∫ f(x) dx` (단, `c`는 상수)  

<br>

- 부정적분과 관련하여 다음 공식이 성립한다. 각 공식에서 `C`는 적분상수를 의미한다.  
  - (1) `∫ α dx` = `αx + C` (단, `α`는 상수)
  - (2) `∫ x`<sup>`k`</sup>` dx` = `x`<sup>`k+1`</sup>`/(k+1) + C` (단, `k` ≠ `-1`)
  - (3) `∫ 1/x dx` = `ln |x| + C`
  - (4) `∫ e`<sup>`x`</sup>` dx` = `e`<sup>`x`</sup>` + C`
  - (5) `∫ a`<sup>`x`</sup>` dx` = `a`<sup>`x`</sup>`/(ln a) + C` (단, `a`는 상수)  
  - (6) `∫ cos x dx` = `sin x + C`
  - (7) `∫ sin x dx` = `-cos x + C`
  - (8) `∫ sec`<sup>`2`</sup>` x dx` = `tan x + C`
  - (9) `∫ csc`<sup>`2`</sup>` x dx` = `-cot x + C`
  - (10) `∫ sec x tan x dx` = `sec x + C`  
  - (11) `∫ csc x cot x dx` = `-csc x + C`  
  - (12) `∫ 1/√(1 - x`<sup>`2`</sup>`) dx` = `sin`<sup>`-1`</sup>` x + C`  
  - (13) `∫ 1/(x`<sup>`2`</sup>`+ 1) dx` = `tan`<sup>`-1`</sup>` x + C`  

<br>

### 2. 치환적분과 부분적분
- 치환적분 : 함수 `f(x)`가 구간 `I`에서 연속이고, 함수 `u` = `g(x)`의 공역이 `I`이며 미분가능하다고 할 때, 다음이 성립한다.  
`∫ f(g(x)) g'(x) dx = ∫ f(u) du`  

<br>

- 부분적분 : 두 함수 `f(x)`, `g(x)`가 미분가능할 때, 다음이 성립한다.  
`∫ f(x) g'(x) dx = f(x) g(x) - ∫ f'(x) g(x) dx`  
만약 `u` = `f(x)`, `v` = `g(x)`라 하면, 위 식은 다음과 같이 간단히 나타낼 수 있다.  
`∫ u dv = uv - ∫ v du`  

<br>

### 3. 정적분
- 함수 `f(x)`가 구간 `[a, b]`에서 정의된다고 할 때, 임의의 자연수 `n`에 대하여 구간 `[a, b]`를 `n`등분하고, 각 구간의 길이를 `Δx` = `(b - a)/n`이라 하면, `1` ≤ `i` ≤ `n`에 대하여 `x`<sub>`i`</sub><sup>`*`</sup>를 각 구간에 있는 표본점이라고 하자. 그러면 `a`부터 `b`까지의 함수 `f(x)`의 **정적분**은 다음과 같이 정의된다.  
![definite_integral][def]  
위와 같은 정적분이 존재할 때, 함수 `f(x)`가 구간 `[a, b]`에서 **적분가능하다**고 한다.  

<br>

- 정적분의 성질 1 : 두 함수 `f(x)`, `g(x)`가 구간 `[a, b]`에서 정의된다고 할 때, 다음이 성립한다.  
![properties_of_definite_integral_1][def2]  

- 정적분의 성질 2 : 두 함수 `f(x)`, `g(x)`가 구간 `[a, b]`에서 정의된다고 할 때, 다음이 성립한다.  
![properties_of_definite_integral_2][def3]  

- 미적분학의 제1기본정리 : 함수 `f(x)`가 구간 `[a, b]`에서 연속이고, 구간 `(a, b)`에서 미분가능하다고 하자.  
![F(x)_definition][def4] 라고 할 때, 다음이 성립한다.  
`F'(x) = f(x)`  

- 미적분학의 제2기본정리 : 함수 `f(x)`가 구간 `[a, b]`에서 연속이고, `(a. b)`에서 미분가능하며, 함수 `f(x)`의 역도함수를 `F(x)`라 할 때, 즉 `F'(x) = f(x)`일 때 다음이 성립한다.  
![fundamental_theorem_of_calculus_2][def5]  

<br>

- 적분의 중간값 정리 : 구간 `[a, b]`에서 정의된 연속함수 `f(x)`에 대하여,  
![mean_value_theorem_of_integration][def6]을 만족하는 `c` ∈ `[a, b]`가 존재한다.  

<br>

- 정적분의 치환적분 : 함수 `f`가 구간 `I`에서 연속이고, `u` = `g(x)`의 공역이 `I`이며 미분가능하다고 할 때, 다음이 성립한다.  
![definite_integral_substitution][def7]  

<br>

### 4. 적분의 응용  
- 곡선의 선분 길이 : 함수 `f`가 구간 `[a, b]`에서 미분가능하고 `f'`이 연속일 때, 곡선 `y` = `f(x)` (`a` ≤ `x` ≤ `b`)의 선분이 길이 `L`은 다음과 같다.  
![arc_length][def8]  

<br>

- 회전체의 표면적 1 : 곡선 `y` = `f(x)`가 구간 `a` ≤ `x` ≤ `b`에서 양의 값을 갖고 `f'`이 연속이라고 하자. `x`축 기준으로 이 곡선을 회전시켜 얻은 회전체의 표면적 `S`는 다음과 같다.  
![surface_area_1][def9]  
![solid_of_revolution][def10]  

- 회전체의 표면적 2 : 곡선 `x` = `g(y)`가 구간 `c` ≤ `y` ≤ `d`에서 양의 값을 갖고 `g'`이 연속이라고 하자. `y`축 기준으로 이 곡선을 회전시켜 얻은 회전체의 표면적 `S`는 다음과 같다.  
![surface_area_2][def11]  

<br>
<br>
<br>
<br>
<details>
<summary>주의사항</summary>
<div markdown=   "1">

이 포스팅은 강원대학교 이구연 교수님의 인공지능수학 수업을 들으며 내용을 정리 한 것입니다.  
수업 내용에 대한 저작권은 교수님께 있으니,  
다른 곳으로의 무분별한 내용 복사를 자제해 주세요.

</div>
</details> 

[def]: https://i.imgur.com/V15PlTX.png
[def2]: https://i.imgur.com/WmZjoKM.png
[def3]: https://i.imgur.com/LuiqdKj.png
[def4]: https://i.imgur.com/RoF7see.png
[def5]: https://i.imgur.com/9nyVS2g.png
[def6]: https://i.imgur.com/3S97Hb6.png
[def7]: https://i.imgur.com/3bJ54DB.png
[def8]: https://i.imgur.com/FfPDHCX.png
[def9]: https://i.imgur.com/U4ktnCQ.png
[def10]: https://i.imgur.com/biisAqv.png
[def11]: https://i.imgur.com/A0LBaz0.png